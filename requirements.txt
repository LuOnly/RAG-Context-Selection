# ===== RAG 上下文选择优化系统 依赖 =====
# 核心科学计算
numpy>=1.24

# 嵌入模型（相关性得分 v_i）。若环境无法安装，系统会自动回退到 TF-IDF。
sentence-transformers>=2.2.0

# Token 计数（重量 w_i）。若无法安装，系统会自动回退到启发式计数。
tiktoken>=0.5.0

# 绘图（评估指标可视化）。若无法安装，系统会自动以纯文本打印结果。
matplotlib>=3.7

# 可选：用于回退方案的 TF-IDF 向量化（sentence-transformers 不可用时使用）
scikit-learn>=1.3

# 可选：加载 MS MARCO 等标准数据集
datasets>=2.14
